server:
  port: 83

spring:
  application:
    name: nacos-order-consumer
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

# 用于配置负载均衡后的服务名
service-url:
  nacos-user-service: http://nacos-service-provider